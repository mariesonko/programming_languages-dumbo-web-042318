def reformat_languages(languages)
  new_hash =[]
    ruby.each {|key, value|}
    if new_hash.include ? (k)
    languages[:ruby][:style] << style
  else
    key.each {|string, val|}
    new_hash[:ruby]= {:type => val , :style => [style]}}
  end
  new_hash
end
